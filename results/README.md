# Benchmark results

This directory is reserved for sanitized CSV files produced by `scripts/benchmark_ollama.sh`.

Before committing a result:

- Confirm it contains timing metrics only.
- Verify the model name does not contain a private filesystem path.
- Record hardware and runtime changes in the commit message.
- Do not add prompts, generated responses, usernames, tokens, or private configuration.

